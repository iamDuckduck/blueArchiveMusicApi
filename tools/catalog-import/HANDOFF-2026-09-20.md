# Welcome back — catalog work checkpoint

## The short version

1. **Pipeline:** the manual scan → prepare → review → publish flow is ready for
   your review, with clearer guidance and safer refresh/retry behavior.
2. **Next stage, kept separate:** structured character/CV credits, aliases and
   credit-aware music search now work in an isolated test environment.
3. **Thanks to:** category is **Blue Archive Global songs**. The EN/KR versions
   are a named collection of official songs, not a claim that a formal album exists.

Your original Thanks to draft is prepared but **not selected or published to
development R2**. A disposable copy passed local publishing, playback and retry
checks. Production, old catalog/media and the original dirty backend worktree
were left alone. No branches were merged or pushed.

## Where the work lives

Both folders are Git worktrees: separate working folders for the same backend
repository. You can inspect both without repeatedly switching one folder's files.

| Branch | Working folder | Purpose |
| --- | --- | --- |
| `review/catalog-import-approved` | Retained branch at `2624d3a` | Your reviewed chapters 1–17; unchanged |
| `feature/catalog-pipeline-finish` | `catalog-import-review` | Six follow-up commits, ending at `a9d3c95` |
| `feature/catalog-credits-search` | `catalog-credits-search` | Starts at `a9d3c95`; next-stage work only above that point |

The original `blueArchiveMusicApi` folder remains on `feature/catalog-import-tool`
with its pre-existing `CatalogImportService.java` working change preserved. The
deployment-notes worktree was not used. Parent `docs/` is outside backend Git;
its current-decisions page links here, while this handoff is committed.

### Pipeline commits, in reading order

| Commit | Plain-English purpose |
| --- | --- |
| `9c02a9a` | Record the samples, your rebuild decision and official-song scope |
| `1ebed2a` | Validate source evidence and show reader limitations honestly |
| `882728c` | Keep publication approval safe through changes, failures and restarts |
| `d53fbb4` | Explain the operator's next action and support official-song collections |
| `8e1d74a` | Test another prepared collection without changing your original draft |
| `a9d3c95` | Document the operator flow and verified checkpoint |

### Separate credits/search commits

| Commit | Plain-English purpose |
| --- | --- |
| `f354ed8` | Keep structured character/CV names and shared aliases in the backend |
| `a7a1365` | Find songs/albums through directly credited names and aliases |
| `ba07d15` | Add the dark alias editor with an explicit backend Save action |
| `e4c34d2` | Verify credits/search and retries in a fresh local database |
| Documentation commit containing this page | Explain the operator workflow and handoff |

## Try the results

These are temporary local previews, left running for inspection. Check the port:

| Open | What it uses |
| --- | --- |
| [Your Thanks to draft](http://127.0.0.1:8770/albums/d7f743d6-1cf3-48fa-886a-a1da0df10a68/) | Original saved review; development API 18083/R2 destination; no new publication |
| [Normal development app](http://localhost:5173/library/albums/1) | Veritas only, development-check database and development R2 |
| [Thanks to playback test](http://127.0.0.1:15173/library/albums/9) | API 18082, existing local verification PostgreSQL/MinIO |
| [Credit alias editor](http://127.0.0.1:8772/credits) | API 18084, fresh `catalog_credits_verify_0a1b59fd`, local MinIO |
| [Credit-search music app](http://127.0.0.1:15174/library) | Same fresh credits-test database; search `Chihiro demo` |

For the alias example: search `千寻` in the alias editor and select
`千寻 / 山村響`. Its saved test-only alias `Chihiro demo` finds the vocal and
associated instrumental in the music app. It does not pull in unrelated BGM.
Typing aliases does not save; **Save aliases to backend** updates the shared
profile, not your local album draft. Aliases do not merge identities.

The fresh credits database contains two imported Veritas tracks and one explicitly
uncredited BGM control record used to test exclusion. This is test data, not a
replacement catalog. Original development data was not migrated to V1.11.

## What was checked

- Pipeline branch: 128 Python tests, 5 JS tests, 19 backend tests passed; one
  opt-in real-R2 test skipped. Veritas failure/restart/conflict/retry checks passed.
- Thanks to: both versions played in the existing frontend. Repeating publication
  kept IDs, revisions, counts and media timestamps unchanged. Original SQLite and
  all five media hashes stayed unchanged during verification.
- Credits branch: 146 Python tests, 5 JS tests, 31 backend tests passed; one R2
  test skipped. Fresh PostgreSQL V1.1–V1.11 migration replay and live search passed.
- Character/CV/composer/alias/title/album queries worked; duplicate credit matches
  returned songs once; unrelated BGM was excluded. Alias edits and identical
  reimport preserved IDs/revisions/media. Unauthenticated alias edits were rejected.
- Browser alias Save persisted across reload and worked in the existing music
  app's search. The small UI follows explicit local state and Save actions, with
  no autosave or background profile polling.

Evidence is retained under each worktree's ignored `target/`:

- Pipeline: `catalog-live-20260920-200114/report.json`,
  `catalog-candidate-20260920-200708-436524a6/report.json`,
  `pipeline-check-20260920/thanks-to-playback.png`.
- Credits: `credits-live-20260920-202738-5e2ca8/report.json`,
  `credits-preview/alias-editor.png`, `credits-preview/alias-search.png`.

Do not run `mvn clean`: retained reviews, screenshots and the development backup
also live in `target/`. Reproduction instructions are in [setup](README.md) and
[the credits guide](CREDITS-GUIDE.md). A running preview occupies the verifier's
port, so stop that owned preview before rerunning its verifier.

## What is not finished, and the next action

**GameKee's full-content CDN still returns HTTP 567.** The reader shows partial
evidence/errors and permits cached/manual reference notes; it does not bypass the
restriction. This is a usable manual-review pipeline, not fully unattended import.

The whole catalog has not been validated or rebuilt. Search ranking/large-catalog
performance were not stress-tested. R2 CORS remains deferred as you requested;
native Veritas playback passed earlier, but fetch/Web Audio access is not proven.

Next, review these branches and the prepared Thanks to fields, especially music
type/credits. Unknown values remain unknown. Select approved tracks and publish
to development only when you are satisfied. Then rebuild the catalog deliberately,
release by release, through the new pipeline; legacy source-ID backfill is no
longer the plan. Keep the old 45-album/295-song database and its backup until an
explicit replacement decision.

After the catalog is validated: Google sign-in/private likes and playlists, then
AWS/domain launch. Those stages were not started or deployed in this run.

For less technical reading, start with [the operator guide](OPERATOR-GUIDE.md);
use [progress](PIPELINE.md) for the full history and limits.
